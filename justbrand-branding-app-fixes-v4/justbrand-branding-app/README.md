# JustBrand Branding Configurator — Fixes (v4)
Drop these files into your repo to fix the Vercel build:
- `apps/web/app/layout.tsx`
- `apps/web/next.config.js`
- `apps/web/package.json` (runs Prisma during build)
